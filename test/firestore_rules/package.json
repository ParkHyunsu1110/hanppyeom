{
  "name": "hanppyeom-firestore-rules-test",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore 보안 규칙 검증 (Firebase Emulator + @firebase/rules-unit-testing)",
  "scripts": {
    "test": "node run.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
